<% unless card.nil? or card.prev_status.nil? %>
    <%= form_for [card.board, card] do |f| %>
        <%= hidden_field :card, :status, value: card.prev_status %>
        <%= f.submit raw('&larr;'), class: "btn btn-secondary btn-sm" %>
    <% end %>
<% end %>